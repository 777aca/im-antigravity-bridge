version: '3.8'

services:
  bot:
    image: ghcr.io/777aca/im-antigravity-bridge:latest
    container_name: feishu-bot
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
