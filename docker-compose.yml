version: '3.8'

services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feishu-bot
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
